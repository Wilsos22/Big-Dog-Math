// Public wrapper for the demo run-through: the SAME Support projector
// component that runs the classroom, reachable without the teacher gate.
// Safe by construction - see /demo/present.
export { default } from "@/app/teacher/pace/page";
